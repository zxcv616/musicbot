[project]
name = "lyric-video-backend"
version = "0.0.0"
description = "Local Whisper transcription endpoint for the lyric video app"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "faster-whisper>=1.1.0",
]
